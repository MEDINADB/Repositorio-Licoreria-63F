@model IEnumerable<LiqourStore.Entities.Tienda>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DireccionTienda)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DireccionTienda)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TiendaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TiendaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TiendaId })
        </td>
    </tr>
}

</table>
